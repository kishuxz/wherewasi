node_modules/
dist/
*.tsbuildinfo
.DS_Store
.context/

# Local secrets. .env.example is the committed template; .env never is.
.env
.env.*
!.env.example

# Evaluation output — real captured sessions, not for the repo.
eval/out/
